# Saved copies of the psiu.org pages the build reads. Regenerated by
# ./build/build.sh --pull, so there is no need to keep them in the repository.
crawl/

# Throwaway input for the search indexer (about 40 MB). Rebuilt on every run.
build/index_html/

# Local scratch
_to_delete/
*.tar.gz
_update*.tar.gz

# OS clutter
.DS_Store
Thumbs.db
desktop.ini
